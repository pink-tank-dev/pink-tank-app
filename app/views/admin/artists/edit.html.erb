<div class="container mt-2 mb-2">
  <%= link_to "Back", admin_artist_path(@artist) %>
  <h4>Edit <%= @artist.name %></h4>
  <% if @artist.avatar.attached? %>
    <%= image_tag @artist.avatar.variant(resize: "100x100") %>       
  <% end %>
  <%= render partial: 'form' %>
</div>
